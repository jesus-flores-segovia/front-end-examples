document.addEventListener("DOMContentLoaded", () => {
    /* window.onscroll = () => {
        const navbar = document.getElementById("navbar");
        var navbar_topOffset = navbar.offsetTop;
    
        window.scrollY > navbar_topOffset ? navbar.classList.add("sticky") : navbar.classList.remove("sticky");
    }; */
});